dist
coverage
node_modules
public/data/pricing.json
docs/pricing-changelog.md

# Generated: the daily pipeline writes these, and `wrangler types` rewrites the
# last one on every worker typecheck. Reformatting machine output only creates
# diffs nobody asked for.
worker/node_modules
worker/dist
worker/worker-configuration.d.ts
api/node_modules
api/dist
api/worker-configuration.d.ts
mcp/node_modules
mcp/dist
vscode/node_modules
vscode/dist
# npm writes this; reformatting a lockfile is churn at best and confuses npm
# at worst. The same reasoning as the generated worker types above.
mcp/package-lock.json
vscode/package-lock.json
**/.wrangler
test-results
playwright-report

# Expo Router and Metro generate these locally. Device screenshots and bundle
# manifests are release evidence, not author-maintained source.
apps/mobile/.expo
apps/mobile/expo-env.d.ts

# Local traffic exports are diagnostic evidence and may be intentionally kept
# outside version control. Never rewrite them during a repository format gate.
referrers-*.json
